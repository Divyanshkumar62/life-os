package com.lifeos.penalty.domain.enums;

public enum FailureReason {
    FAILED,        // Player explicitly failed
    EXPIRED,       // Deadline passed
    ABANDONED      // Player manually dropped quest
}
