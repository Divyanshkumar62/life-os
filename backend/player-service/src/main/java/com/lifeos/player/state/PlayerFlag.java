package com.lifeos.player.state;

public enum PlayerFlag {
    PENALTY_ZONE,
    STREAK_BROKEN,
    REWARDS_SUPPRESSED,
    XP_FROZEN,
    WARNING_STATUS // Added based on PenaltyAdvancementTest logic interactions
}
